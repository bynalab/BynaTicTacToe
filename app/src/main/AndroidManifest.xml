<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bynalab.bynatictactoe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tbyt"></activity>
        <activity android:name=".option"></activity>
        <activity android:name=".singleplayer"></activity>
        <activity android:name=".leaderboard"></activity>
        <activity android:name=".multiplayer"></activity>
        <activity android:name=".settings"></activity>
        <activity android:name=".multi_tbyt"></activity>
        <activity android:name=".multi_fbyf"></activity>


    </application>

</manifest>